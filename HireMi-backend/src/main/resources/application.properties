spring.application.name=HireMi
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# MongoDB Atlas Configuration
spring.data.mongodb.uri=mongodb+srv://thdarklord12345_db_user:8mcZ329BgAm30p81@cluster0.wcwqpgi.mongodb.net/clearhire?retryWrites=true&w=majority&appName=Cluster0
spring.data.mongodb.database=clearhire

server.port=${SERVER_PORT:8080}

jwt.secret=${JWT_SECRET:mySecretKey123456789012345678901234567890}
jwt.expiration=86400000

cors.allowed.origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}
openai.api.key=${OPENAI_API_KEY:your_openai_api_key_here}

# Actuator endpoints
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always